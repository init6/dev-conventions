## コミット / ブランチ規約

### ブランチ
- 作業開始時に、最新のデフォルトブランチ（多くのリポジトリは `develop`）を取得し、そこから分岐する。
- ブランチ名には対応する Redmine のチケット番号を含める（例: `task/604`）。

### コミットメッセージ
- 先頭に対応する Redmine チケット番号を `refs #<番号>` 形式で付け、続けてタスクのタイトルを書く。

  ```
  refs #604 タスクのタイトル
  ```

- コミット / PR は、ユーザーから明示的に依頼されたときのみ行う。
